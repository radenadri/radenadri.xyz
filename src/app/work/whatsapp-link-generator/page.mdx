export const metadata = {
  title: 'WhatsApp Link Generator — Case Study',
  description: 'Micro utility to compose WhatsApp chat links with country-aware inputs, copy-to-clipboard, and instant launch.',
};

import { AnimatedElement, AnimatedText } from '@/components/animated-element';
import Image from 'next/image';

<section className="space-y-12 text-sm leading-relaxed">
  <div className="relative overflow-hidden rounded-[2.5rem] border border-white/10 bg-gradient-to-br from-white/5 via-white/0 to-white/10 p-10">
    <div className="absolute inset-0 -z-10 bg-[radial-gradient(circle_at_top,_rgba(255,255,255,0.15),_transparent_55%)]" />
    <p className="font-data text-xs tracking-[0.6em] text-[var(--space-red-shift)] uppercase">
      [ CASE STUDY / 02 ]
    </p>
    <h1 className="mt-4 font-wide text-4xl sm:text-6xl uppercase leading-[1.05]">
      WhatsApp Link Generator
    </h1>
    <p className="mt-6 max-w-3xl font-data text-base text-white/80">
      A single-page tool that lets people start a WhatsApp chat without saving the number first.
      Users pick a country code, type the phone number, optionally pre-fill a message, and share
      a clean <code>wa.me</code> link instantly.
    </p>
  </div>

  <div className="grid gap-8 lg:grid-cols-[1.2fr_1fr]">
    <div className="space-y-6 rounded-3xl border border-white/10 bg-white/5 p-8">
      <p className="font-data text-xs tracking-[0.5em] text-[var(--space-blue-shift)] uppercase">
        [ description ]
      </p>
      <p>
        Turn the hidden <code>wa.me</code> deep link into a guided UX so sales reps, community managers, and
        support teams can share a WhatsApp entry point that feels intentional and on-brand.
      </p>
      <p>
        The page had to stay featherweight, mobile-first, and instantly understandable: two inputs, one
        preview, and a clear CTA.
      </p>
    </div>

    <div className="space-y-4 rounded-3xl border border-white/10 bg-white/5 p-8">
      <p className="font-data text-xs tracking-[0.5em] text-[var(--space-red-shift)] uppercase">
        [ how it works ]
      </p>
      <ul className="list-disc space-y-3 pl-5">
        <li>Split the phone input into a country code and subscriber number to reduce formatting errors.</li>
        <li>Keep the message optional but preserve whitespace so users can craft templates.</li>
        <li>Mirror the generated <code>https://wa.me/&lt;number&gt;</code> URL live in a read-only field.</li>
        <li>Offer one-click copy and a direct “Send Message” button that opens WhatsApp Web or the native app.</li>
      </ul>
    </div>
  </div>

  <div className="rounded-3xl border border-white/10 bg-white/5 p-8">
    <p className="font-data text-xs tracking-[0.5em] text-[var(--space-blue-shift)] uppercase">
      [ live demo ]
    </p>
    <p className="mt-3 text-white/80">Open the production build:</p>
    <div className="mt-4 mb-6 flex flex-wrap gap-3 text-xs uppercase">
      <a
        className="rounded-full border border-white/20 px-4 py-2 font-data tracking-[0.3em] text-white/80"
        href="https://whatsapp.radenadri.xyz/"
        target="_blank"
        rel="noopener noreferrer"
      >
        whatsapp.radenadri.xyz ↗
      </a>
    </div>
  </div>
</section>
