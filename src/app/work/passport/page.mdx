export const metadata = {
  title: 'Passport — Case Study',
  description: 'Modern attendance solution to make employee attendance easy and secure.',
};

import { AnimatedElement, AnimatedText } from '@/components/animated-element';
import Image from 'next/image';

<section className="space-y-12 text-sm leading-relaxed">
  <div className="relative overflow-hidden rounded-[2.5rem] border border-white/10 bg-gradient-to-br from-white/5 via-white/0 to-white/10 p-10">
    <div className="absolute inset-0 -z-10 bg-[radial-gradient(circle_at_top,_rgba(255,255,255,0.15),_transparent_55%)]" />
    <p className="font-data text-xs tracking-[0.6em] text-[var(--space-red-shift)] uppercase">
      [ CASE STUDY / 02 ]
    </p>
    <h1 className="mt-4 font-wide text-4xl sm:text-6xl uppercase leading-[1.05]">
      Passport — HRIS
    </h1>
    <p className="mt-6 max-w-3xl font-data text-base text-white/80">
      A biometric + GPS attendance system shipped at Pentacode to keep employees in sync with
      HR, payroll, and compliance.
    </p>
    <div className="mt-8 grid gap-4 text-xs sm:grid-cols-3">
      <div className="rounded-2xl border border-white/10 p-4">
        <p className="text-white/60">Role</p>
        <p className="font-semibold">Frontend (Flutter) Developer</p>
      </div>
      <div className="rounded-2xl border border-white/10 p-4">
        <p className="text-white/60">Stack</p>
        <p className="font-semibold">Flutter · Firebase · Laravel APIs</p>
      </div>
      <div className="rounded-2xl border border-white/10 p-4">
        <p className="text-white/60">Timeline</p>
        <p className="font-semibold">2024</p>
      </div>
    </div>
  </div>

  <div className="grid gap-8 lg:grid-cols-[1.2fr_1fr]">
    <div className="space-y-6 rounded-3xl border border-white/10 bg-white/5 p-8">
      <p className="font-data text-xs tracking-[0.5em] text-[var(--space-blue-shift)] uppercase">
        [ mission brief ]
      </p>
      <p>
        Replace the paper-and-spreadsheet attendance workflow with a trusted mobile companion that can
        verify employees via biometrics, GPS fences, and real-time policy engines.
      </p>
      <p>
        Passport centralizes daily check-ins, leave approvals, overtime tracking, and HR dashboards so
        every team works from the same data source.
      </p>
    </div>

    <div className="space-y-4 rounded-3xl border border-white/10 bg-white/5 p-8">
      <p className="font-data text-xs tracking-[0.5em] text-[var(--space-red-shift)] uppercase">
        [ flight plan ]
      </p>
      <ul className="list-disc space-y-3 pl-5">
        <li>Ship a Flutter app with fingerprint + face ID check-ins.</li>
        <li>Integrate Laravel REST APIs and JWT auth for secure sessions.</li>
        <li>Deliver push notifications through Firebase Cloud Messaging.</li>
        <li>Expose attendance analytics for HR and managers in near real time.</li>
      </ul>
    </div>
  </div>

  <AnimatedElement animation="stagger">
    <div className="grid gap-4 sm:grid-cols-2">
      {[{
        label: 'Manual corrections',
        value: '-50%',
        detail: 'Less time spent fixing missed punches',
      },
      {
        label: 'HR processing speed',
        value: '+60%',
        detail: 'Approvals + exports accelerated after launch',
      }].map((stat) => (
        <div key={stat.label} className="rounded-3xl border border-white/10 bg-white/5 p-6">
          <AnimatedText as="p" className="font-data text-xs uppercase tracking-[0.3em] text-white/60">
            {stat.label}
          </AnimatedText>
          <AnimatedText as="p" className="mt-2 text-3xl font-wide">
            {stat.value}
          </AnimatedText>
          <p className="mt-2 text-white/70">{stat.detail}</p>
        </div>
      ))}
    </div>
  </AnimatedElement>

  <div className="rounded-[2.5rem] border border-white/10 bg-white/5 p-8">
    <p className="font-data text-xs tracking-[0.5em] text-[var(--space-red-shift)] uppercase">
      [ cockpit visuals ]
    </p>
    <AnimatedElement animation="stagger" className="mt-6 space-y-6">
      <Image
        className="rounded-[1.5rem] border border-white/10"
        width={1600}
        height={900}
        src="/viz/passport-1.png"
        alt="Passport attendance mobile screens"
      />
    </AnimatedElement>
  </div>

  <div className="grid gap-8 lg:grid-cols-2">
    <div className="rounded-3xl border border-white/10 bg-white/5 p-8">
      <p className="font-data text-xs tracking-[0.5em] text-[var(--space-red-shift)] uppercase">
        [ learnings ]
      </p>
      <ul className="mt-4 space-y-3">
        <li>Combining biometric and GPS validation enhances both accuracy and user trust.</li>
        <li>Centralized attendance and HR workflows drastically reduce administrative overhead.</li>
        <li>Deployment with Firebase Cloud Messaging for push notifications.</li>
        <li>Upload APK and IPA to Google Play and App Store.</li>
      </ul>
    </div>
  </div>

  <div className="rounded-3xl border border-white/10 bg-white/5 p-8">
    <p className="font-data text-xs tracking-[0.5em] text-[var(--space-blue-shift)] uppercase">
      [ deployment ]
    </p>
    <p className="mt-3 text-white/80">Available on both app stores for Pentacode employees:</p>
    <div className="mt-4 flex flex-wrap gap-3 text-xs uppercase">
      <a
        className="rounded-full border border-white/20 px-4 py-2 font-data tracking-[0.3em] text-white/80"
        href="https://play.google.com/store/apps/details?id=id.pentacode.portal&hl=en"
        target="_blank"
        rel="noopener noreferrer"
      >
        Android ↗
      </a>
      <a
        className="rounded-full border border-white/20 px-4 py-2 font-data tracking-[0.3em] text-white/80"
        href="https://apps.apple.com/id/app/passport/id6478735731"
        target="_blank"
        rel="noopener noreferrer"
      >
        iOS ↗
      </a>
    </div>
    <p className="mt-4 text-xs text-white/60">
      *App access is restricted to Pentacode employees, but the listing remains public.
    </p>
  </div>

  <AnimatedElement animation="fadeIn" className="flex justify-between items-center pt-10 border-t border-white/10">
    <a href="/" className="font-data text-sm uppercase tracking-[0.3em] text-[var(--space-red-shift)]">
      ← Back to home
    </a>
  </AnimatedElement>
</section>
